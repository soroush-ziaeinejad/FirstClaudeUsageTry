# Tiny smoke-test config — runs fast to verify the full pipeline works
dataset: cifar10
num_clients: 10
clients_per_round: 4
num_rounds: 5
alpha: 0.5
local_epochs: 1
lr: 0.01
batch_size: 64
data_dir: ./data
cpus_per_client: 2
gpus_per_client: 0.0
torch_num_threads: 2
ray_object_store_mb: 4096
